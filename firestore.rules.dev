// Rules for development environment
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Cho phép đọc/ghi tất cả collections cho mục đích phát triển
      allow read, write: if true;
    }
  }
}
